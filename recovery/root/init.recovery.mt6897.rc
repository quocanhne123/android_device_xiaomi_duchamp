import /keymint.service.mitee.rc
import /tee-supplicant.rc
import /miteelog.rc
import /vibrator.haptic.hal.rc

on init
    setprop sys.usb.configfs 1
    setprop sys.usb.controller "11201000.usb0"
    setprop sys.usb.ffs.aio_compat 1
    export LD_LIBRARY_PATH /system/lib64:/vendor/lib64:/vendor/lib64/hw:/system/lib64/hw
    setprop crypto.ready 1

on fs
    install_keyring
    start boot-hal-1-2
    symlink /dev/block/platform/bootdevice /dev/block/bootdevice

on early-fs
    start vold
    # Quan trọng: Đợi TEE và KeyMint sẵn sàng trước khi decrypt
    start vendor.keymint-mitee
    start vendor.gatekeeper_mitee

on late-fs
    mount_all /first_stage_ramdisk/fstab.mt6897 --late

# [GIỮ NGUYÊN PHẦN CÒN LẠI CỦA FILE init.recovery.mt6897.rc]
# ... các service haptic, mtk_plpath_utils giữ nguyên ...